<%= form_with(model: accumulator, local: true) do |form| %>
  <% if accumulator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(accumulator.errors.count, "error") %> prohibited this accumulator from being saved:</h2>

      <ul>
      <% accumulator.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="field">
    <%= form.label :uplink_id %>
    <%= form.text_field :uplink_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
