<%= form_with(model: valve_position, local: true) do |form| %>
  <% if valve_position.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(valve_position.errors.count, "error") %> prohibited this valve_position from being saved:</h2>

      <ul>
      <% valve_position.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="field">
    <%= form.label :uplink_id %>
    <%= form.text_field :uplink_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
